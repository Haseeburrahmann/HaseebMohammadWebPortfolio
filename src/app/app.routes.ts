import { Routes } from '@angular/router';

export const routes: Routes = [
  // Empty routes array since this is a single-page application
  // All navigation is handled by smooth scrolling to sections
];
